interface IMaps {
    display: string[][];
    displayDir: number[][];
    grid: number[][];
    metadata: Array<Array<null | number>>;
    paths: number[][];
    exit: number[];
    spawnpoints: number[][];
    bg: number[];
    border: number;
    borderAlpha: number;
    cols: number;
    rows: number;
}